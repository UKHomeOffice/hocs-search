search.redrive.policy={"maxReceiveCount": "${search.queue.maximumRedeliveries}", "deadLetterTargetArn":"arn:aws:sqs:${aws.sqs.region}:${aws.account.id}:${search.queue.dlq.name}"}
search.queue=aws-sqs://arn:aws:sqs:${aws.sqs.region}:${aws.account.id}:${search.queue.name}?amazonSQSClient=#sqsClient&messageAttributeNames=All&redrivePolicy=${search.redrive.policy}&waitTimeSeconds=0&defaultVisibilityTimeout=30&visibilityTimeout=30&extendMessageVisibility=true&messageRetentionPeriod=345600&backoffIdleThreshold=1&backoffMultiplier=5&initialDelay=5000&delay=${poll.delay}
search.queue.dlq=aws-sqs://arn:aws:sqs:${aws.sqs.region}:${aws.account.id}:${search.queue.dlq.name}?amazonSQSClient=#sqsClient&messageAttributeNames=All
