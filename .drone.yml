---
kind: pipeline
type: kubernetes
name: deploy tag
trigger:
  event:
    - tag
  branch:
    - main

steps:
  - name: checkout
    image: quay.io/ukhomeofficedigital/hocs-helm-kubectl:1.1.0
    commands:
      - git fetch --tags
      - git checkout ${DRONE_TAG}
      - git submodule update --init --recursive

  - &deploy
    name: cs-dev
    image: quay.io/ukhomeofficedigital/hocs-helm-kubectl:1.1.0
    environment:
      CHART_NAME: hocs-search
      CHART_VERSION: ^5.0.0
      KUBE_CLUSTER: acp-notprod
      KUBE_NAMESPACE: cs-dev
      KUBE_SERVER: https://kube-api-notprod.notprod.acp.homeoffice.gov.uk
      KUBE_TOKEN:
        from_secret: cs_notprod
      VERSION: ${DRONE_TAG}
    commands:
      - ./ci/helm/helm-chart-deploy.sh --values ./helm/hocs-search.yaml
    depends_on:
      - checkout

  - <<: *deploy
    name: wcs-dev
    environment:
      KUBE_NAMESPACE: wcs-dev
    depends_on:
      - cs-dev

  - <<: *deploy
    name: cs-dev-opensearch
    environment:
      CHART_NAME: hocs-search-opensearch
      CHART_VERSION: ^2.0.0
    depends_on:
      - wcs-dev

  - <<: *deploy
    name: wcs-dev-opensearch
    environment:
      KUBE_NAMESPACE: wcs-dev
      CHART_NAME: hocs-search-opensearch
      CHART_VERSION: ^2.0.0
    depends_on:
      - cs-dev-opensearch

  - &deploy-consumer
    name: cs-dev-consumer
    image: quay.io/ukhomeofficedigital/hocs-helm-kubectl:1.1.0
    environment:
      CHART_NAME: hocs-search-consumer
      CHART_VERSION: ^4.0.0
      KUBE_CLUSTER: acp-notprod
      KUBE_NAMESPACE: cs-dev
      KUBE_SERVER: https://kube-api-notprod.notprod.acp.homeoffice.gov.uk
      KUBE_TOKEN:
        from_secret: cs_notprod
      VERSION: ${DRONE_TAG}
    commands:
      - ./ci/helm/helm-chart-deploy.sh --values ./helm/hocs-search-consumer.yaml
    depends_on:
      - checkout

  - <<: *deploy-consumer
    name: wcs-dev-consumer
    environment:
      KUBE_NAMESPACE: wcs-dev
    depends_on:
      - cs-dev-consumer

  - <<: *deploy-consumer
    name: cs-dev-consumer-opensearch
    environment:
      CHART_NAME: hocs-search-consumer-opensearch
      CHART_VERSION: ^2.0.0
    depends_on:
      - wcs-dev-consumer

  - <<: *deploy-consumer
    name: wcs-dev-consumer-opensearch
    environment:
      CHART_NAME: hocs-search-consumer-opensearch
      CHART_VERSION: ^2.0.0
      KUBE_NAMESPACE: wcs-dev
    depends_on:
      - cs-dev-consumer-opensearch
